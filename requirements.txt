# Core data science
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.12.0
polars>=0.20.0

# Baseball data
pybaseball>=2.2.7

# Learned models (station E's gradient-boosted challenger). Pinned: the
# walk-forward numbers in docs/market-benchmark-2026.md were produced by
# these versions, and a boosting library's defaults move between minors.
scikit-learn==1.7.2
lightgbm==4.6.0

# Bayesian modeling (Phase 2+)
pymc>=5.10.0
arviz>=0.17.0
# pytensor  # installed with pymc
# numpyro  # optional JAX backend

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0

# Data storage
pyarrow>=15.0.0
fastparquet>=2024.2.0

# Database (Turso Cloud)

# Experiment tracking
wandb>=0.19.0

# Utilities
tqdm>=4.66.0
requests>=2.31.0

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0

# Dev
ipython>=8.20.0
jupyter>=1.0.0
