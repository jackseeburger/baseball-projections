# Minimal deps for CI and fresh cloud sessions: enough to run the test
# suite and the eval harness. Heavy modeling deps (pymc, arviz) stay in
# requirements.txt — MCMC runs on Modal, not in CI.
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.12.0
pyarrow>=15.0.0
requests>=2.31.0
pytest>=8.0.0
boto3>=1.34.0
# Station E's learned challenger. Both are wheels on every CI platform and
# add ~10 s to the install; the tests that import them train a few hundred
# rows, never the real table (scripts/train_game_learned.py does that).
scikit-learn==1.7.2
lightgbm==4.6.0
