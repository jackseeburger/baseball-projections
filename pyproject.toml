[project]
name = "baseball-projections"
version = "0.1.0"
description = "Bayesian Baseball Projection System"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
